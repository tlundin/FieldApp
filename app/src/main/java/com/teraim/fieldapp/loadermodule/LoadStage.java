package com.teraim.fieldapp.loadermodule;

// An enum to identify the type of job, useful for debugging and logging.
public enum LoadStage {
    FILES,
    DATABASES,
    // You could add more types later, e.g., USER_PREFERENCES
}
